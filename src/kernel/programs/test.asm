test:
    ; mov bx, 2
    ; mov ax, 2
    ; call power

    ; add ax, 48
    ; call write_char
    ; call new_line
    ; call new_line

    ; mov ax, 6
    ; call binary_decimal
    ; call new_line
    ; call new_line

    ; mov bx, number_input
    ; call decimal_binary
    ; call binary_decimal
    ; call new_line
    ; call new_line

    call calculator

    ret

; number_input: db "249", 0
